# Censorarr GPU Worker

The optional GPU Worker is maintained as a standalone Docker project under `gpu-worker/`.

See [`gpu-worker/README.md`](gpu-worker/README.md) for requirements, installation, configuration, health checks, and updates.
