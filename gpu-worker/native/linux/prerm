#!/bin/sh
set -e
if command -v systemctl >/dev/null 2>&1 && [ -d /run/systemd/system ]; then
    systemctl stop censorarr-gpu-worker.service >/dev/null 2>&1 || true
    systemctl disable censorarr-gpu-worker.service >/dev/null 2>&1 || true
fi
exit 0
